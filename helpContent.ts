
export const helpContent = {
  dashboard: `
# 📊 监控大盘操作手册

监控大盘是 RiskGuard 风控系统的"驾驶舱"，旨在帮助风控运营人员（Risk Ops）和 SRE 工程师实时掌握系统的运行健康状况。

---

## 1. 核心指标解读

顶部四张卡片展示了系统当前的生命体征：

| 指标名称 | 含义说明 | 健康参考 | 异常处理建议 |
| :--- | :--- | :--- | :--- |
| **生效策略数** | 当前系统中状态为 \`已发布\` 的所有熔断和护栏策略总和。 | - | 若数量突降，检查是否有批量误操作或配置同步失败。 |
| **当前熔断数** | 正在处于 \`打开(OPEN)\` 状态的熔断器数量（含自动熔断+手动干预）。 | 0 ~ 2 | **高危**。若数值 > 0，请立即查看下方"最近告警"确认是哪个服务触发了熔断。 |
| **系统 QPS** | 风控引擎每秒处理的决策请求总数。 | 视业务而定 | 若曲线断崖式下跌，检查上游接入层；若激增，关注系统负载。 |
| **特征缺失率** | 核心特征（如历史交易金额）获取失败或为空的比例。 | < 0.1% | 反映数据上游（特征服务/数据库）的健康度。升高请联系数据组。 |

---

## 2. 流量与异常趋势图
此图表展示过去 30 分钟内的流量动态，用于识别突发问题。

*   **紫色区域 (总 QPS)**: 代表系统的吞吐量。
*   **红色曲线 (异常数)**: 代表触发熔断或系统错误的请求数。
    *   **关注点**: 如果红色曲线与紫色区域同步上升，可能是流量过载；如果紫色平稳但红色激增，可能是下游服务故障。

---

## 3. 实时告警流
右下角展示系统生成的实时事件流，按严重程度分类：
*   🔴 **Critical (严重)**: 服务熔断触发。意味着部分特征降级或请求被拒，需立即响应。
*   🟡 **Warning (警告)**: 业务指标波动（如某规则命中率激增），可能需要关注。
*   🔵 **Info (信息)**: 审计日志，如"管理员手动关闭了熔断器"。

`,

  'circuit-breakers': `
# ⚡ 服务熔断策略配置手册

## 📖 功能定义
**服务熔断 (Circuit Breaker)** 主要保护**技术依赖**的稳定性。
当风控引擎调用的外部服务（如特征平台 HTTP 接口、模型服务、三方数据源）出现高延迟或高失败率时，熔断器会自动切断调用，并返回预设的默认值，防止"雪崩效应"拖垮整个风控系统。

---

## 🛠 操作步骤：新建熔断策略

点击页面右上角的 **[+ 新建策略]** 按钮，按照以下逻辑配置：

### 第一步：定义保护对象 (Scope)
*   **层级 (Level)**: 通常选择 \`FEATURE_SERVICE\` (特征服务)。
*   **目标标识 (Target Key)**: 输入具体的特征名称或接口名。
    *   *示例*: \`user_transaction_history\` (用户交易历史特征)。
    *   *提示*: 输入框支持联想搜索，输入字符即可看到系统中已注册的特征。

### 第二步：设置触发阈值
熔断器基于时间窗口（滑动窗口）内的统计数据判断是否打开。

*   **失败率阈值**: 建议设为 \`50%\`。即 100 次调用中有 50 次失败（超时或报错），则熔断。
*   **慢调用率**: 建议设为 \`50%\`。
*   **慢调用判定时间**: 定义什么是"慢"。例如 \`500ms\`，超过此时间的请求被视为慢调用。
*   **统计窗口**: 例如 \`60秒\`。收集最近 60 秒的数据进行计算。
*   **最小请求数**: 例如 \`100\`。窗口内请求数少于此值时，即使失败率高也不会熔断（防止小流量误判）。

### 第三步：配置降级动作 (Degrade Action)
当熔断发生时，系统如何处理后续请求？

> **🌟 最佳实践**: 选择 \`DEFAULT_VALUE\` (回填默认值)。
>
> **原因**: 如果因为查不到"交易历史"就直接拒绝用户，体验极差。回填一个默认值（如 0），让规则引擎继续运行，虽然风险识别精度下降，但业务流程不受阻。

---

## 🔍 状态流转机制
1.  **CLOSED (关闭)**: ✅ 服务健康，请求正常通行。
2.  **OPEN (打开)**: ❌ 达到阈值，熔断器打开，请求被立即拦截并执行降级动作。
3.  **HALF_OPEN (半开)**: ⏳ 经过冷却时间（如 30秒）后，允许少量请求通过。如果这些请求成功，恢复为 CLOSED；否则重新变为 OPEN。

`,

  guardrails: `
# 🛡️ 业务护栏策略配置手册

## 📖 功能定义
**业务护栏 (Guardrails)** 关注**业务指标**的合理性。
它不关心接口是否超时，只关心"是不是拦截了太多人"或"某条规则是不是失效了"。它是防止配置错误（Human Error）导致业务大规模受损的最后一道防线。

---

## 🛠 配置核心逻辑

### 1. 选择监控指标 (Metric)
*   **PASS_RATE (通过率)**: 针对事件接入点（如 \`EP0001\` 交易接口）。防止因为系统 Bug 导致所有用户都被拒绝。
*   **RULE_HIT_RATE (规则命中率)**: 针对单条规则（如 \`warmUpRule01\`）。防止规则配置过于宽松或误杀。

### 2. 设定比较方式 (Comparator)
护栏通常通过对比**当前值**与**基线值**（过去一段时间的均值）来触发。

| 比较方式 | 适用场景 | 示例配置 |
| :--- | :--- | :--- |
| **DROP_PERCENT (下跌幅度)** | 监控通过率暴跌 | 阈值 \`50\` (即下跌了50%) |
| **RISE_PERCENT (上涨幅度)** | 监控拦截率/命中率暴涨 | 阈值 \`200\` (即翻了3倍) |
| **LT (小于) / GT (大于)** | 绝对值监控 | 阈值 \`0.1\` (通过率低于10%) |

### 3. 定义执行动作
当指标异常时，系统能自动做什么？

*   **SWITCH_FALLBACK_SET (切换兜底)**: 🌟 *最常用*。自动将该接入点的流量切换到一套"兜底策略集"（通常只包含黑名单），确保业务不中断但拦截最明显的风险。
*   **RULE_DISABLE (禁用规则)**: 自动将出问题的规则置为无效。
*   **REQUIRE_REVIEW (转人工)**: 仅告警，并将请求打标，强制流转至人工审核队列。

---

## 💡 场景示例
**场景：新上线的"年龄<18"规则配置错误，导致拦截了所有人。**

1.  **监控对象**: 选择该规则名称。
2.  **触发条件**: \`RULE_HIT_RATE\` (命中率) + \`RISE_PERCENT\` (上涨幅度) > \`300\`。
3.  **执行动作**: \`RULE_DISABLE\` (禁用规则)。
4.  **效果**: 系统检测到该规则命中率异常飙升（比平时高3倍），自动禁用该规则，止损并发送钉钉告警。

`,

  overrides: `
# ⚠️ 手动干预操作手册 (War Room)

## 📖 功能定义
手动干预（Overrides）拥有**最高优先级**。
它绕过所有的自动计算逻辑（阈值、窗口），直接对目标对象生效。**仅在系统故障、网络专线中断或重大已确认风险时使用。**

---

## 🛠 操作流程

### 1. 新建干预
在"手动干预管理"页面点击 **[新建紧急干预]**。

*   **生效范围**:
    *   **全局 (GLOBAL)**: 影响整个风控系统，慎用！
    *   **策略集 (ACTIVATION)**: 针对特定场景（如"登录风控"）。
    *   **特征 (FEATURE_SERVICE)**: 针对特定数据源。

*   **干预动作**:
    *   ⛔ **MANUAL_OPEN (强制熔断)**: **"止血"操作**。直接阻断请求或返回默认值。适用于确认某服务返回脏数据污染决策时。
    *   ✅ **MANUAL_CLOSE (强制放行)**: **"放水"操作**。忽略所有错误，强行通过。适用于自动熔断器误判，影响了核心业务（如双11大促）时。

*   **TTL (有效期)**:
    *   必须设置有效期（如 600秒）。
    *   干预措施属于临时手段，不应长期存在。过期后系统自动恢复自动托管状态。

### 2. 移除干预
问题解决后，点击卡片上的 **[移除干预]** 按钮，系统将立即恢复自动控制。

---

## ❓ 常见问题 (FAQ)

*   **Q: 如果同时存在自动熔断和手动强制关闭，谁生效？**
    *   A: **手动强制关闭生效**。优先级：手动干预 > 自动熔断 > 正常逻辑。
*   **Q: TTL 结束后会发生什么？**
    *   A: 配置自动删除。系统会立即根据当前的实时统计数据（流量、错误率）重新判断应该处于 Open 还是 Closed 状态。
*   **Q: 谁有权限操作？**
    *   A: 此功能通常仅向 SRE 和风控负责人开放，且所有操作都会记录审计日志。
`,

  'feature-list': `
# 🧬 特征平台管理操作手册

## 📖 什么是风控特征 (Risk Feature)?
在风控系统中，**特征**是决策的核心"变量"。它是对原始数据进行清洗、加工、统计后得到的指标。
*   **示例**:
    *   \`user_login_count_1h\`: 用户最近1小时登录次数（聚合特征）。
    *   \`device_is_root\`: 设备是否越狱（直接提取特征）。
    *   \`last_txn_location\`: 最近一次交易地点（历史存储特征）。

特征平台负责统一管理这些变量的定义、计算逻辑（实时/离线）、存储方式以及版本发布。

---

## 🖥️ 特征列表页功能详解

特征列表是特征管理的主入口，支持全生命周期的管理。

### 1. 高级筛选区
顶部提供了多维度的筛选器，用于快速定位特征：

*   **特征名**: 支持模糊搜索（如输入 \`login\` 可搜到 \`user_login_count\`）。
*   **特征描述**: 支持搜索中文业务描述。
*   **特征类型**:
    *   **直接存储 (Direct)**: 从实时事件流中直接提取并存储，无需复杂计算。
    *   **历史存储 (History)**: 存储历史快照（如用户最近5次交易记录）。
    *   **聚合特征 (Aggregation)**: 基于时间窗口的统计（如 Count, Sum, Max）。
    *   **离线存储 (Offline)**: 来源是数仓（Hive/MaxCompute），通常是 T+1 数据。
*   **发布状态 (Lifecycle State)**:
    *   **已发布 (Published)**: 当前线上正在运行的稳定版本。
    *   **待发布 (Ready)**: 已提交测试，等待加入发布单的版本。
    *   **草稿 (Draft)**: 正在编辑中，未提交的版本。
*   **启用状态**: 控制运行时是否实际执行计算。

### 2. 列表核心字段
| 字段 | 说明 |
| :--- | :--- |
| **特征信息** | 显示特征的唯一标识符（Key）和中文描述。点击特征名可进入详情页。 |
| **数据来源** | \`实时写入\` (接口触发) 或 \`异步补齐\` (消息队列延迟处理)。 |
| **发布状态** | 标识当前特征处于研发流程的哪个阶段。 |
| **启用状态** | **已启用 (Green)**: 线上正常计算。<br>**已禁用 (Red)**: 线上跳过计算，返回空值。 |

---

## 🕹️ 核心操作逻辑 (非常重要)

### 1. 查看特征 (智能路由逻辑)
点击特征名称或"查看"按钮时，系统会执行**智能路由**：
*   **逻辑**: 如果该特征存在 \`已发布 (Published)\` 的版本，系统默认展示线上版本详情；如果该特征是纯新增的，没有线上版本，则展示 \`草稿 (Draft)\` 或 \`待发布 (Ready)\` 版本。
*   **目的**: 确保运营人员优先看到的是"当前线上实际在跑的配置"，避免被开发中的草稿误导。

### 2. 编辑特征 (版本分叉)
*   **逻辑**: 风控特征**严禁直接修改线上版本**。
*   **行为**: 当你点击"编辑"时，如果当前没有草稿，系统会基于当前线上版本 (或历史最新版) **Fork (分叉)** 出一个新的 \`草稿 (Draft)\` 版本。
*   **后续**: 所有的修改都只保存在草稿中，直到你点击"提交待发布"并走完发布流程，新版本才会覆盖线上版本。

### 3. 批量操作 (Batch Actions)
勾选列表左侧的复选框，顶部会出现批量操作栏：

*   **🛒 批量待发布 (Release)**:
    *   **仅针对草稿**: 系统会自动过滤掉非草稿状态的特征。
    *   **作用**: 将选中的草稿状态变更为 \`Ready\`，并自动生成一条变更记录加入"待发布清单"。
    *   **场景**: 批量开发了10个特征，测试通过后，一次性提交等待发布。

*   **▶️ 批量启用 / ⏹️ 批量禁用**:
    *   **注意**: 修改启用状态**本质上也是一次变更**。
    *   **逻辑**: 系统会为选中的特征生成一个新的 \`草稿\`，修改其 \`status\` 字段。你需要后续提交发布才能生效。它不会直接修改线上运行状态（为了安全）。

*   **🗑️ 批量删除**:
    *   **安全机制**: 仅能删除 \`草稿 (Draft)\`。
    *   **逻辑**: 如果一个特征有线上版本，点击删除只会删除你正在编辑的草稿，**不会影响线上业务**。只有当特征从未上线过（纯草稿），才会被彻底物理删除。

---

## 📝 特征编辑器字段详解

编辑器分为三个核心区域，每个字段都决定了特征的计算逻辑。

### 区域一：基础信息
| 字段 | 必填 | 说明 |
| :--- | :--- | :--- |
| **特征名** | 是 | 全局唯一标识（如 \`user_level\`）。**创建后不可修改**，因为下游规则引用的是此 Key。 |
| **启用状态** | 是 | 控制开关。关闭后，即使规则引用了该特征，引擎也会直接返回 null，不执行耗时的计算逻辑。 |
| **数据类型** | 是 | 返回值的类型（String, Integer, Boolean, List 等）。配置错误会导致规则比较失败。 |

### 区域二：数据源与计算配置
此区域根据**特征类型**不同而动态变化：

*   **通用字段**:
    *   **触发事件接入点**: 定义在哪些业务节点（如"交易前"、"提现后"）触发此特征计算。
    *   **包含当前事件**: 计算聚合值时，是否把当前这笔正在处理的请求也算进去（例如：计算"含本次的当日累计金额"）。

*   **聚合特征 (Aggregation) 专属**:
    *   **聚合方法**: Sum(求和), Count(计数), Max(最大), Distinct_Count(去重) 等。
    *   **时间窗口**: 统计过去多久的数据（秒）。如 \`86400\` 代表24小时。
    *   **来源特征**: 聚合是基于基础特征计算的。例如"近1小时交易总额"依赖于"交易金额"这个基础特征。

*   **直接/历史存储 (Direct/History) 专属**:
    *   **数值提取路径 (JsonPath)**: 如何从原始事件报文中提取数据。例如 \`properties.data.amount\`。
    *   **TTL 有效期**: 数据存储多久后自动过期删除。

*   **离线存储 (Offline) 专属**:
    *   **源表信息**: 对应数仓中的 Hive 表名、字段名。
    *   **分区策略**: 指定 T+1 数据在哪个分区（如 \`dt=20231027\`）。

### 区域三：核心逻辑配置
*   **前置条件表达式**:
    *   **作用**: 前置过滤。满足所有条件时，才会执行后续复杂的计算/存储逻辑。
    *   **场景**: 仅当 \`fact.amount > 50000\` 时，才记录一条"大额交易流水"。
    *   **条件组**: 支持多条件组合，组内支持 AND/OR 逻辑，组间支持 AND/OR 逻辑。
    *   **字段**: 可使用 \`fact.*\` 访问事件字段，\`context.*\` 访问全局上下文。

*   **复合键维度配置 (Composite Keys)**:
    *   **作用**: 定义特征的"主键"（Group By Key）。
    *   **示例**: 计算"**每个用户**在**每个设备**上的登录次数"。需配置两个 Key：\`userId\` 和 \`deviceId\`。

---

## 🔄 生命周期流转图 (Lifecycle)

\`DRAFT (草稿)\` 📝
  ↓ *(提交待发布)*
\`READY (待发布)\` 🛒
  ↓ *(发布单审批通过)*
\`PUBLISHED (已发布/线上)\` ✅
  ↓ *(新版本发布覆盖)*
\`ARCHIVED (历史归档)\` 📦

*   **Tips**: 在详情页底部，你可以看到该特征所有的历史版本记录（History）。支持随时查看任意历史快照，并支持基于历史版本"回滚"（即基于旧版本重新生成一个新草稿）。
`
};
