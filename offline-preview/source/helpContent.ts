export const helpContent = {
  dashboard: `
# 📊 监控大盘操作手册

监控大盘是 RiskGuard 风控系统的“驾驶舱”，帮助 Risk Ops 和 SRE 实时掌握系统健康度。

---

## 1. 指标卡片
| 指标 | 解释 | 告警信号 | 处理建议 |
| :--- | :--- | :--- | :--- |
| 生效策略数 | 当前处于 已发布 状态的熔断 + 护栏策略数量 | 数量突然下降 | 是否有批量误操作或同步失败 |
| 当前熔断数 | 状态为 OPEN 的熔断器数量 | > 0 | 立即定位触发的服务，评估影响范围 |
| 系统 QPS | 决策引擎每秒处理的请求 | 突然断崖或激增 | 检查上游接入层/容量 | 
| 特征缺失率 | 核心特征读取失败比例 | > 0.1% | 上游特征服务异常，联系数据组 |

---

## 2. 流量曲线
- **紫色区域：总 QPS**，用于识别流量波峰。
- **红色折线：异常数**，显示熔断/错误数量。
- **联动分析**：紫色稳定但红线激增 → 下游服务故障；两者同步上升 → 流量过载。

---

## 3. 实时事件流
| 类型 | 说明 | 行动 |
| :- | :- | :- |
| 🔴 Critical | 服务熔断、系统不可用 | 立即响应，必要时切换兜底策略 |
| 🟡 Warning | 指标波动、命中率异常 | 监控趋势，必要时启动临时护栏 |
| 🔵 Info | 审计日志、人工操作 | 复盘用，无需即时操作 |

`,

  'circuit-breakers': `
# ⚡ 服务熔断策略

## 功能定位
熔断策略保护技术依赖（特征服务、模型、三方接口）不至于拖垮整体链路。当失败率或响应时间过高时，自动切断调用并返回预设默认值。

---

## 配置流程
1. **定义保护对象**：选择 FEATURE_SERVICE 等层级，填写 Target Key（可模糊搜索）。
2. **设定阈值**：失败率、慢调用率、窗口时间、最小请求数等指标必须组合使用，避免低流量误判。
3. **降级策略**：推荐 DEFAULT_VALUE，保证业务流程继续；也可配置 FALLBACK_INTERFACE、BLOCK 等动作。
4. **告警与通知**：可配置钉钉、邮件等渠道，方便 SRE 及时处理。

---

## 状态机
CLOSED ➜ (触发阈值) ➜ OPEN ➜ (冷却后) HALF_OPEN ➜ 成功则 CLOSED，否则回到 OPEN。半开允许少量流量探测服务是否恢复。

`,

  guardrails: `
# 🛡️ 业务护栏策略

护栏关注业务指标（规则命中率、通过率、拒绝率等）是否异常，防止配置错误伤及业务。

## 创建要点
- **监控指标**：PASS_RATE、RULE_HIT_RATE、BLOCK_RATIO 等。
- **窗口设置**：时间窗口、最小样本，避免因低流量误报。
- **阈值类型**：绝对阈值 / 环比变化 / 滑动基线。
- **动作**：提示、强制走兜底集、触发人工审核、暂停策略等。

## 常见场景
- 大促期间突然拒绝率升高 → 设置 PASS_RATE 护栏。
- 某规则命中率持续为 0 → 设置 RULE_HIT_RATE 护栏，提醒回归测试。

`,

  overrides: `
# 🛠️ 手动干预（Override）

适用于紧急期的人工开关控制，可快速放量或熔断。

| 字段 | 说明 |
| :--- | :--- |
| Scope & Level | 支持 Activation / Rule / Feature 级别 |
| ManualState | MANUAL_OPEN (强制熔断) / MANUAL_CLOSE (强制恢复) |
| TTL | 自动失效时间，防止遗漏回收 |
| Remark | 必填，描述原因和观察项 |

操作会记录审计日志，建议仅 SRE 和负责人持有权限。

`,

  'feature-list': `
# 🧬 特征平台管理手册

## 0. 风控系统概览
- **接入点（Event Point）**：规则引擎中的事件入口，代表业务节点（如“交易前风险评估”）。特征、规则、策略都会声明自己适用的接入点。
- **特征（Feature）**：特征平台加工得到的指标（如 7 天登录次数），是规则引擎决策的原材料。
- **规则（Rule）**：在规则引擎中编排的 Groovy 条件 + 评分逻辑，对特征做判断。
- **策略（Strategy/Activation）**：规则集合，描述某个接入点下的整体决策流程。
- **特征平台 / 规则引擎 / 数据平台**：分别负责特征建模与版本管理、规则执行与编排、以及原始数据的采集与脱敏。

## 1. 📖 什么是风控特征
特征是决策引擎的核心“变量”，通过清洗、聚合、统计原始数据得到。平台负责定义、计算、存储、发布及回滚。

### 示例
- \`user_login_count_1h\`：用户最近 1 小时登录次数（聚合）。
- \`device_is_root\`：设备是否越狱（直接提取）。
- \`last_txn_location\`：最近一次交易地点（历史存储）。

## 2. 🖥️ 列表页功能详解
特征列表是全生命周期管理的入口。

### 2.1 高级筛选区
- **特征名 / 描述**：支持英文 Key 和中文描述模糊搜索。
- **特征类型**：直接存储、历史存储、聚合、离线存储。
- **发布状态**：草稿、待发布、已发布、历史归档。
- **启用状态**：控制运行时是否执行计算。
- **接入点多选**：一键过滤指定业务节点的全部特征。

### 2.2 核心字段字典
| 字段 | 说明 |
| :--- | :--- |
| 特征信息 | Key + 中文描述，点击进入详情。 |
| 特征类型 | Direct / History / Aggregation / Offline。 |
| 数据来源 | REALTIME（实时写入）或 ASYNC（异步补齐）。 |
| 接入点 | 展示可触发该特征的事件点。 |
| 发布状态 | 标识研发流程阶段。 |
| 启用状态 | 胶囊标签提示“已启用 / 已禁用 / 配置中”。 |
| 更新时间 / 操作人 | 最近一次保存时间 + 作者，平台永久记录创建人、更新人以便审计。 |

## 3. 🕹️ 核心操作逻辑
1. **智能查看**：优先展示已发布版本；若无上线版本，则展示草稿/待发布版本，避免误读实验配置。
2. **编辑（版本分叉）**：禁止直接改线上；点击“编辑”会自动 fork 草稿，所有修改仅保存在草稿，提交待发布后才覆盖线上。
3. **批量操作**：  
   - **批量待发布**：仅草稿可提交，提交后状态改为 Ready，并生成待发布清单记录。  
   - **批量启用/禁用**：实质是生成草稿修改 status，需要走发布流程。  
   - **批量删除**：只删除草稿，不影响线上；纯草稿特征才会被彻底删除。

## 4. 📝 特征编辑器字段

### 区域一：基础信息
| 字段 | 必填 | 说明 |
| :--- | :--- | :--- |
| 特征名 | 是 | 全局唯一，下游规则与策略引用的 Key，创建后禁止修改。 |
| 启用状态 | 是 | 控制开关，关闭后直接返回 null。 |
| 数据类型 | 是 | String/Integer/Boolean/List 等，决定规则比较方式。 |

### 区域二：数据源与计算配置
- **通用字段**：触发接入点、写入方式、是否包含当前事件。
- **聚合特征专属**：聚合方法、时间窗口、来源特征、复合键必须成套配置；勾选“包含当前事件”时需配合去重键。
- **直接/历史存储**：填写 JsonPath 后可启用脱敏策略；不同存储引擎展示不同 TTL/列族输入。
- **离线存储**：填写 db.table 后自动拉取 schema；指定分区策略时可定义延迟补数窗口。

### 区域三：核心脚本与维度
- **条件脚本（Groovy）**：前置过滤，仅返回 true 时才执行后续逻辑，可通过 \`fact\` 访问事件参数。
- **复合键配置**：决定 Group By 维度，如“用户 + 设备”。

## 5. 🔄 生命周期
\`DRAFT\` (草稿) 📝  
 ↓（提交待发布）  
\`READY\` (待发布) 🛒  
 ↓（发布单审批通过）  
\`PUBLISHED\` (已发布) ✅  
 ↓（新版本覆盖）  
\`ARCHIVED\` (历史归档) 📦

Tips：详情页展示完整历史，可随时回看或基于历史版本生成新草稿。
`,

  'release-candidates': `
# 📝 待发布清单

## 目标
承接各模块的草稿，供发布负责人打包、校验，是“上线前的缓冲区”。

## 表头说明
| 列 | 说明 |
| :- | :- |
| 对象类型 | “特征 / 规则 / 策略”三类，对应特征平台与规则引擎里的草稿对象 |
| 对象名称 / ID | 唯一标识。点击可在抽屉中查看底层配置（特征/规则/策略详情） |
| 接入点 | 与规则引擎的 eventPoint 保持一致（如 EP00000001），便于确认触发场景 |
| 版本 | 草稿内部版本号，如 v4 |
| 变更摘要 | 研发填写，描述影响范围 |
| 更新时间 / 提交人 | 最新保存时间及作者 |

## 操作流程
1. 勾选条目 → 点击「加入发布单」。
2. 填写发布单标题、影响范围、风险评估、回滚方案。
3. 提交后条目从清单移除，进入“发布单列表”。
4. 如需撤回草稿，可点操作列的“删除”。仅作者或管理员可删除。

## 审核 Checklist
- 变更摘要是否完整（目的/影响/验证）。
- 依赖项是否齐全（例如特征与策略需一起提交）。
- 是否标注灰度策略、监控项。
- 若是禁用/删除操作，确认下游依赖已下线。

`,

  'release-orders': `
# 🚀 发布单列表

## 功能
记录审批、执行、验证的全流程，是合规与审计的核心实体。

## 字段
| 字段 | 说明 |
| :- | :- |
| 发布单号 | 系统自动生成，如 REL-20251101-002 |
| 标题 | 建议格式：场景-对象-动作（如 双11-特征批量-灰度上线） |
| 包含对象 | 展示“特征 / 规则 / 策略”数量及关键名称，入口会联动到底层配置 |
| 状态 | PENDING → APPROVING → RUNNING → SUCCESS/FAILED |
| 申请人 / 审批人 | 标注责任链路 |
| 时间线 | 申请、审批、执行完成时间 |

## 操作
1. **审批**：在详情中审阅对象列表、风险评估，选择通过或驳回（驳回后草稿回到待发布清单）。
2. **执行**：执行人员点击“开始执行”，系统记录日志；可配置灰度参数、观察指标。
3. **监控**：执行中可查看实时进度、成功/失败详情，必要时暂停或回滚。
4. **完成**：成功后生成执行报告并推送通知；失败则需填写原因并决定是否补救。

## 建议
- 同一业务活动合并到一张发布单，便于审批与回溯。对象类型字段会标注“特征 / 规则 / 策略”，明确上线内容。
- 在发布单描述中附上方案链接、验证脚本、监控地址。接入点字段需与规则引擎 / 特征平台的配置保持一致，方便审批人对应到后端逻辑。
- 导出执行报告进行复盘或存档。
`
};
